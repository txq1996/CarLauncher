<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:orientation="vertical">

    <!-- 合并了原来的 content_root + page_content 两层 LinearLayout -->
    <LinearLayout
        android:id="@+id/page_content"
        android:layout_width="match_parent"
        android:layout_height="0px"
        android:layout_weight="1"
        android:orientation="vertical"
        android:padding="10px">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0px"
            android:layout_weight="1"
            android:orientation="horizontal">

            <!-- ── 左侧栏：模块容器（卡片由 HomeModules 按注册表填充） ── -->
            <LinearLayout
                android:id="@+id/left_col"
                android:layout_width="260px"
                android:layout_height="match_parent"
                android:orientation="vertical" />

            <Space android:id="@+id/gap_col" android:layout_width="10px" android:layout_height="match_parent" />
            <include layout="@layout/card_pip" />

            <!-- ── 右侧栏：模块容器（歌词 / 后续可扩展区块） ── -->
            <Space android:id="@+id/gap_right_col" android:layout_width="10px" android:layout_height="match_parent" />
            <LinearLayout
                android:id="@+id/right_col"
                android:layout_width="250px"
                android:layout_height="match_parent"
                android:orientation="vertical" />
        </LinearLayout>

        <Space android:id="@+id/gap_dock" android:layout_width="match_parent" android:layout_height="10px" />
        <include layout="@layout/bar_dock" />
    </LinearLayout>
</LinearLayout>
